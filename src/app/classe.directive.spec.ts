import { ClasseDirective } from './classe.directive';

describe('ClasseDirective', () => {
  it('should create an instance', () => {
    const directive = new ClasseDirective();
    expect(directive).toBeTruthy();
  });
});
